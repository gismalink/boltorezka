name: datowave

services:
  datowave-turn:
    image: coturn/coturn:4.6.3
    container_name: datowave-turn-test-n-prod
    restart: unless-stopped
    user: "0:0"
    command:
      - sh
      - -ec
      - |
        TURN_LISTEN_IP_ARG=""
        TURN_RELAY_IP_ARG=""
        TURN_VERBOSE_ARG=""
        if [ -n "$${TURN_LISTEN_IP:-}" ]; then
          TURN_LISTEN_IP_ARG="--listening-ip=$${TURN_LISTEN_IP}"
        fi
        if [ -n "$${TURN_RELAY_IP:-}" ]; then
          TURN_RELAY_IP_ARG="--relay-ip=$${TURN_RELAY_IP}"
        fi
        if [ "${TURN_VERBOSE:-1}" = "1" ]; then
          TURN_VERBOSE_ARG="--verbose"
        fi

        TURN_CERT_DOMAIN_ARG="${TURN_CERT_DOMAIN:-datowave.com}"
        TURN_CERT_FILE_ARG="${TURN_CERT_FILE:-/turn-certs/$${TURN_CERT_DOMAIN_ARG}.crt}"
        TURN_KEY_FILE_ARG="${TURN_KEY_FILE:-/turn-certs/$${TURN_CERT_DOMAIN_ARG}.key}"

        exec turnserver \
          --no-cli \
          --simple-log \
          --listening-port=3478 \
          --tls-listening-port=${TURN_TLS_PORT:-5349} \
          --fingerprint \
          --lt-cred-mech \
          --realm=${TURN_REALM:-datowave.com} \
          --external-ip=${TURN_EXTERNAL_IP:-95.165.154.118} \
          $${TURN_LISTEN_IP_ARG} \
          $${TURN_RELAY_IP_ARG} \
          --user="${TURN_USERNAME}:${TURN_PASSWORD}" \
          --cert=$${TURN_CERT_FILE_ARG} \
          --pkey=$${TURN_KEY_FILE_ARG} \
          --min-port=${TURN_MIN_PORT:-30000} \
          --max-port=${TURN_MAX_PORT:-31000} \
          $${TURN_VERBOSE_ARG}
    ports:
      - "3478:3478/udp"
      - "3478:3478/tcp"
      - "${TURN_TLS_PORT:-5349}:${TURN_TLS_PORT:-5349}/tcp"
      - "${TURN_MIN_PORT:-30000}-${TURN_MAX_PORT:-31000}:${TURN_MIN_PORT:-30000}-${TURN_MAX_PORT:-31000}/udp"
      - "${TURN_MIN_PORT:-30000}-${TURN_MAX_PORT:-31000}:${TURN_MIN_PORT:-30000}-${TURN_MAX_PORT:-31000}/tcp"
    volumes:
      - ${TURN_CERTS_DIR:-/Volumes/datas3/datowave/turn-certs}:/turn-certs:ro
      - ${TURN_STATE_DIR:-/Volumes/datas3/datowave/turn-state}:/var/lib/coturn
    networks:
      - datowave_public

  datowave-auth-test-datowave:
    image: datowave-auth:test-n-prod
    container_name: datowave-auth-test-datowave
    restart: unless-stopped
    env_file:
      - ./.env.auth.test.datowave
    depends_on:
      - datowave-auth-db-test-datowave
    networks:
      - datowave_public
      - data_test

  datowave-auth-db-test-datowave:
    image: postgres:16
    container_name: datowave-auth-db-test-datowave
    restart: unless-stopped
    environment:
      POSTGRES_DB: auth_test_datowave
      POSTGRES_USER: auth_test_datowave
      POSTGRES_PASSWORD: auth_test_datowave
    volumes:
      - ${AUTH_DB_TEST_DATA_DIR:-/Volumes/datas3/datowave/auth-db/test}:/var/lib/postgresql/data
    networks:
      data_test:
        aliases:
          - datowave-auth-db-test-datowave
          - boltorezka-auth-db-test-datowave

  datowave-auth-prod-datowave:
    image: datowave-auth:test-n-prod
    container_name: datowave-auth-prod-datowave
    restart: unless-stopped
    env_file:
      - ./.env.auth.prod.datowave
    depends_on:
      - datowave-auth-db-prod-datowave
    networks:
      - datowave_public
      - data_prod

  datowave-auth-db-prod-datowave:
    image: postgres:16
    container_name: datowave-auth-db-prod-datowave
    hostname: datowave-auth-db-prod-datowave
    restart: unless-stopped
    environment:
      POSTGRES_DB: auth_prod_datowave
      POSTGRES_USER: auth_prod_datowave
      POSTGRES_PASSWORD: auth_prod_datowave
    volumes:
      - ${AUTH_DB_PROD_DATA_DIR:-/Volumes/datas3/datowave/auth-db/prod}:/var/lib/postgresql/data
    networks:
      data_prod:
        aliases:
          - datowave-auth-db-prod-datowave
          - boltorezka-auth-db-prod-datowave

  datowave-api-test:
    build:
      context: ../apps/api
      dockerfile: Dockerfile
      additional_contexts:
        web: ../apps/web
      args:
        VITE_RTC_ICE_SERVERS_JSON: ${TEST_VITE_RTC_ICE_SERVERS_JSON:-}
        VITE_RTC_ICE_TRANSPORT_POLICY: ${TEST_VITE_RTC_ICE_TRANSPORT_POLICY:-all}
        VITE_RTC_RECONNECT_MAX_ATTEMPTS: ${TEST_VITE_RTC_RECONNECT_MAX_ATTEMPTS:-3}
        VITE_RTC_RECONNECT_BASE_DELAY_MS: ${TEST_VITE_RTC_RECONNECT_BASE_DELAY_MS:-1000}
        VITE_RTC_RECONNECT_MAX_DELAY_MS: ${TEST_VITE_RTC_RECONNECT_MAX_DELAY_MS:-8000}
        VITE_APP_VERSION: ${TEST_VITE_APP_VERSION:-}
        VITE_APP_BUILD_DATE: ${TEST_VITE_APP_BUILD_DATE:-}
        VITE_AUTH_COOKIE_MODE: ${TEST_AUTH_COOKIE_MODE:-1}
        VITE_AUTH_BEARER_STORAGE: ${TEST_VITE_AUTH_BEARER_STORAGE:-memory}
    image: datowave-api:test
    container_name: datowave-api-test
    restart: unless-stopped
    environment:
      PORT: 8080
      DATABASE_URL: postgresql://${TEST_POSTGRES_USER}:${TEST_POSTGRES_PASSWORD}@datowave-db-test:5432/${TEST_POSTGRES_DB}
      REDIS_URL: redis://datowave-redis-test:6379
      JWT_SECRET: ${TEST_JWT_SECRET}
      JWT_EXPIRES_IN: ${TEST_JWT_EXPIRES_IN:-12h}
      CORS_ORIGIN: https://test.datowave.com
      AUTH_MODE: sso
      AUTH_SSO_BASE_URL: https://test.auth.datowave.com
      ALLOWED_RETURN_HOSTS: test.datowave.com
      SUPER_ADMIN_EMAIL: ${TEST_SUPER_ADMIN_EMAIL:-gismalink@gmail.com}
      AUTH_COOKIE_MODE: ${TEST_AUTH_COOKIE_MODE:-1}
      AUTH_SESSION_COOKIE_NAME: ${TEST_AUTH_SESSION_COOKIE_NAME:-datowave_session_test}
      AUTH_SESSION_COOKIE_SECURE: ${TEST_AUTH_SESSION_COOKIE_SECURE:-1}
      AUTH_SESSION_COOKIE_SAMESITE: ${TEST_AUTH_SESSION_COOKIE_SAMESITE:-Lax}
      AUTH_SESSION_COOKIE_DOMAIN: .test.datowave.com
      AUTH_SESSION_COOKIE_PATH: ${TEST_AUTH_SESSION_COOKIE_PATH:-/}
      AUTH_SESSION_COOKIE_MAX_AGE_SEC: ${TEST_AUTH_SESSION_COOKIE_MAX_AGE_SEC:-2592000}
      WS_CALL_DEBUG: ${TEST_WS_CALL_DEBUG:-0}
      APP_VERSION: ${TEST_APP_VERSION:-0.1.0}
      APP_BUILD_SHA: ${TEST_APP_BUILD_SHA:-}
      API_SERVE_STATIC: ${TEST_API_SERVE_STATIC:-0}
      LIVEKIT_ENABLED: ${TEST_LIVEKIT_ENABLED:-1}
      LIVEKIT_URL: ${TEST_LIVEKIT_URL:-wss://test.datowave.com}
      LIVEKIT_API_KEY: ${LIVEKIT_TEST_API_KEY:-}
      LIVEKIT_API_SECRET: ${LIVEKIT_TEST_API_SECRET:-}
      LIVEKIT_TOKEN_TTL_SEC: ${TEST_LIVEKIT_TOKEN_TTL_SEC:-1800}
      CHAT_STORAGE_PROVIDER: ${TEST_CHAT_STORAGE_PROVIDER:-localfs}
      CHAT_MINIO_ENDPOINT: http://datowave-minio-test:9000
      CHAT_MINIO_REGION: ${TEST_CHAT_MINIO_REGION:-us-east-1}
      CHAT_MINIO_ACCESS_KEY: ${TEST_CHAT_MINIO_ACCESS_KEY:-}
      CHAT_MINIO_SECRET_KEY: ${TEST_CHAT_MINIO_SECRET_KEY:-}
      CHAT_MINIO_BUCKET: ${TEST_CHAT_MINIO_BUCKET:-chat-attachments-test}
      CHAT_MINIO_FORCE_PATH_STYLE: ${TEST_CHAT_MINIO_FORCE_PATH_STYLE:-1}
    depends_on:
      datowave-turn:
        condition: service_started
      datowave-db-test:
        condition: service_healthy
      datowave-redis-test:
        condition: service_healthy
    networks:
      - datowave_public
      - data_test

  datowave-db-test:
    image: postgres:16
    container_name: datowave-db-test
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${TEST_POSTGRES_DB}
      POSTGRES_USER: ${TEST_POSTGRES_USER}
      POSTGRES_PASSWORD: ${TEST_POSTGRES_PASSWORD}
    volumes:
      - ${POSTGRES_TEST_DATA_DIR:-/Volumes/datas3/datowave/postgres-db/test}:/var/lib/postgresql/data
      - ../infra/postgres/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U ${TEST_POSTGRES_USER} -d ${TEST_POSTGRES_DB}"
        ]
      interval: 10s
      timeout: 5s
      retries: 10
    networks:
      - data_test

  datowave-redis-test:
    image: redis:7-alpine
    container_name: datowave-redis-test
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 10
    volumes:
      - ${REDIS_TEST_DATA_DIR:-/Volumes/datas3/datowave/redis/test}:/data
    networks:
      - data_test

  datowave-minio-test:
    image: minio/minio:latest
    container_name: datowave-minio-test
    restart: unless-stopped
    profiles: ["minio-test"]
    environment:
      MINIO_ROOT_USER: ${TEST_MINIO_ROOT_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${TEST_MINIO_ROOT_PASSWORD:-minioadmin}
    command: server /data --console-address ":9001"
    ports:
      - "${TEST_MINIO_API_PORT:-19000}:9000"
      - "${TEST_MINIO_CONSOLE_PORT:-19001}:9001"
    volumes:
      - ${MINIO_TEST_DATA_DIR:-/Volumes/datas3/datowave/minio-test}:/data
    networks:
      - data_test

  datowave-minio-test-init:
    image: minio/mc:latest
    container_name: datowave-minio-test-init
    restart: "no"
    profiles: ["minio-test"]
    depends_on:
      datowave-minio-test:
        condition: service_started
    entrypoint:
      - /bin/sh
      - -ec
    command:
      - |
        BUCKET="${TEST_MINIO_BUCKET:-chat-attachments-test}"
        API_USER="${TEST_MINIO_API_USER:-${TEST_CHAT_MINIO_ACCESS_KEY:-}}"
        API_PASSWORD="${TEST_MINIO_API_PASSWORD:-${TEST_CHAT_MINIO_SECRET_KEY:-}}"

        if [ -z "$${API_USER}" ] || [ -z "$${API_PASSWORD}" ]; then
          API_USER="${TEST_MINIO_ROOT_USER:-minioadmin}"
          API_PASSWORD="${TEST_MINIO_ROOT_PASSWORD:-minioadmin}"
          echo "[minio-test-init] warning: TEST_MINIO_API_USER/PASSWORD are not set; fallback to root credentials" >&2
        fi

        attempt=1
        until mc alias set local http://datowave-minio-test:9000 "${TEST_MINIO_ROOT_USER:-minioadmin}" "${TEST_MINIO_ROOT_PASSWORD:-minioadmin}"; do
          if [ "$$attempt" -ge 30 ]; then
            echo "[minio-test-init] failed to reach MinIO after $${attempt} attempts" >&2
            exit 1
          fi
          attempt=$$((attempt + 1))
          sleep 2
        done

        mc mb --ignore-existing "local/$${BUCKET}"
        mc anonymous set none "local/$${BUCKET}"

        mc ilm rule add --expire-delete-marker "local/$${BUCKET}" >/dev/null 2>&1 || true

        cat >/tmp/chat-attachments-rw-policy.json <<EOF
        {
          "Version": "2012-10-17",
          "Statement": [
            {
              "Effect": "Allow",
              "Action": [
                "s3:ListBucket"
              ],
              "Resource": [
                "arn:aws:s3:::$${BUCKET}"
              ]
            },
            {
              "Effect": "Allow",
              "Action": [
                "s3:GetObject",
                "s3:PutObject",
                "s3:DeleteObject"
              ],
              "Resource": [
                "arn:aws:s3:::$${BUCKET}/*"
              ]
            }
          ]
        }
        EOF

        mc admin policy create local chat-attachments-rw /tmp/chat-attachments-rw-policy.json >/dev/null 2>&1 || true
        mc admin user add local "$${API_USER}" "$${API_PASSWORD}" >/dev/null 2>&1 || true
        mc admin policy attach local chat-attachments-rw --user "$${API_USER}" >/dev/null 2>&1 || true
    networks:
      - data_test

  datowave-livekit-test:
    image: livekit/livekit-server:v1.9.12
    container_name: datowave-livekit-test
    restart: unless-stopped
    profiles: ["livekit-test"]
    environment:
      LIVEKIT_TEST_API_KEY: ${LIVEKIT_TEST_API_KEY:-}
      LIVEKIT_TEST_API_SECRET: ${LIVEKIT_TEST_API_SECRET:-}
      LIVEKIT_TEST_SIGNAL_PORT: ${TEST_LIVEKIT_SIGNAL_PORT:-7880}
      LIVEKIT_TEST_TCP_PORT: ${TEST_LIVEKIT_TCP_PORT:-7881}
      LIVEKIT_TEST_RTC_PORT_START: ${TEST_LIVEKIT_RTC_PORT_START:-34000}
      LIVEKIT_TEST_RTC_PORT_END: ${TEST_LIVEKIT_RTC_PORT_END:-34999}
    entrypoint:
      - /bin/sh
      - -ec
    command:
      - |
        if [ -z "${LIVEKIT_TEST_API_KEY:-}" ] || [ -z "${LIVEKIT_TEST_API_SECRET:-}" ]; then
          echo "[livekit-test] LIVEKIT_TEST_API_KEY and LIVEKIT_TEST_API_SECRET must be set" >&2
          exit 1
        fi
        cat > /tmp/livekit.yaml <<EOF
        port: ${TEST_LIVEKIT_SIGNAL_PORT:-7880}
        bind_addresses:
          - "0.0.0.0"
        rtc:
          tcp_port: ${TEST_LIVEKIT_TCP_PORT:-7881}
          port_range_start: ${TEST_LIVEKIT_RTC_PORT_START:-34000}
          port_range_end: ${TEST_LIVEKIT_RTC_PORT_END:-34999}
          use_external_ip: true
        redis:
          address: datowave-redis-test:6379
        logging:
          level: info
        EOF
        exec /livekit-server --config /tmp/livekit.yaml --keys "${LIVEKIT_TEST_API_KEY}: ${LIVEKIT_TEST_API_SECRET}"
    ports:
      - "${TEST_LIVEKIT_SIGNAL_PORT:-7880}:7880/tcp"
      - "${TEST_LIVEKIT_TCP_PORT:-7881}:7881/tcp"
      - "${TEST_LIVEKIT_RTC_PORT_START:-34000}-${TEST_LIVEKIT_RTC_PORT_END:-34999}:${TEST_LIVEKIT_RTC_PORT_START:-34000}-${TEST_LIVEKIT_RTC_PORT_END:-34999}/udp"
    depends_on:
      datowave-redis-test:
        condition: service_healthy
    networks:
      - datowave_public
      - data_test

  datowave-api-prod:
    build:
      context: ../apps/api
      dockerfile: Dockerfile
      additional_contexts:
        web: ../apps/web
      args:
        VITE_RTC_ICE_SERVERS_JSON: ${PROD_VITE_RTC_ICE_SERVERS_JSON:-}
        VITE_RTC_ICE_TRANSPORT_POLICY: ${PROD_VITE_RTC_ICE_TRANSPORT_POLICY:-all}
        VITE_RTC_RECONNECT_MAX_ATTEMPTS: ${PROD_VITE_RTC_RECONNECT_MAX_ATTEMPTS:-3}
        VITE_RTC_RECONNECT_BASE_DELAY_MS: ${PROD_VITE_RTC_RECONNECT_BASE_DELAY_MS:-1000}
        VITE_RTC_RECONNECT_MAX_DELAY_MS: ${PROD_VITE_RTC_RECONNECT_MAX_DELAY_MS:-8000}
        VITE_APP_VERSION: ${PROD_VITE_APP_VERSION:-}
        VITE_APP_BUILD_DATE: ${PROD_VITE_APP_BUILD_DATE:-}
        VITE_AUTH_COOKIE_MODE: ${PROD_AUTH_COOKIE_MODE:-0}
        VITE_AUTH_BEARER_STORAGE: ${PROD_VITE_AUTH_BEARER_STORAGE:-memory}
    image: datowave-api:prod
    container_name: datowave-api-prod
    restart: unless-stopped
    environment:
      PORT: 8080
      DATABASE_URL: postgresql://${PROD_POSTGRES_USER}:${PROD_POSTGRES_PASSWORD}@datowave-db-prod:5432/${PROD_POSTGRES_DB}
      REDIS_URL: redis://datowave-redis-prod:6379
      JWT_SECRET: ${PROD_JWT_SECRET}
      JWT_EXPIRES_IN: ${PROD_JWT_EXPIRES_IN:-12h}
      CORS_ORIGIN: ${PROD_CORS_ORIGIN:-https://datowave.com}
      AUTH_MODE: sso
      AUTH_SSO_BASE_URL: ${PROD_AUTH_SSO_BASE_URL:-https://auth.datowave.com}
      ALLOWED_RETURN_HOSTS: ${PROD_ALLOWED_RETURN_HOSTS:-datowave.com}
      SUPER_ADMIN_EMAIL: ${PROD_SUPER_ADMIN_EMAIL:-gismalink@gmail.com}
      AUTH_COOKIE_MODE: ${PROD_AUTH_COOKIE_MODE:-0}
      AUTH_SESSION_COOKIE_NAME: ${PROD_AUTH_SESSION_COOKIE_NAME:-datowave_session}
      AUTH_SESSION_COOKIE_SECURE: ${PROD_AUTH_SESSION_COOKIE_SECURE:-1}
      AUTH_SESSION_COOKIE_SAMESITE: ${PROD_AUTH_SESSION_COOKIE_SAMESITE:-Lax}
      AUTH_SESSION_COOKIE_DOMAIN: ${PROD_AUTH_SESSION_COOKIE_DOMAIN:-.datowave.com}
      AUTH_SESSION_COOKIE_PATH: ${PROD_AUTH_SESSION_COOKIE_PATH:-/}
      AUTH_SESSION_COOKIE_MAX_AGE_SEC: ${PROD_AUTH_SESSION_COOKIE_MAX_AGE_SEC:-2592000}
      WS_CALL_DEBUG: ${PROD_WS_CALL_DEBUG:-0}
      APP_VERSION: ${PROD_APP_VERSION:-0.1.0}
      APP_BUILD_SHA: ${PROD_APP_BUILD_SHA:-}
      API_SERVE_STATIC: ${PROD_API_SERVE_STATIC:-0}
      LIVEKIT_ENABLED: ${PROD_LIVEKIT_ENABLED:-1}
      LIVEKIT_URL: ${PROD_LIVEKIT_URL:-wss://datowave.com}
      LIVEKIT_API_KEY: ${PROD_LIVEKIT_API_KEY:-}
      LIVEKIT_API_SECRET: ${PROD_LIVEKIT_API_SECRET:-}
      LIVEKIT_TOKEN_TTL_SEC: ${PROD_LIVEKIT_TOKEN_TTL_SEC:-1800}
      CHAT_STORAGE_PROVIDER: ${PROD_CHAT_STORAGE_PROVIDER:-localfs}
      CHAT_MINIO_ENDPOINT: http://datowave-minio-prod:9000
      CHAT_MINIO_REGION: ${PROD_CHAT_MINIO_REGION:-us-east-1}
      CHAT_MINIO_ACCESS_KEY: ${PROD_CHAT_MINIO_ACCESS_KEY:-}
      CHAT_MINIO_SECRET_KEY: ${PROD_CHAT_MINIO_SECRET_KEY:-}
      CHAT_MINIO_BUCKET: ${PROD_CHAT_MINIO_BUCKET:-chat-attachments-prod}
      CHAT_MINIO_FORCE_PATH_STYLE: ${PROD_CHAT_MINIO_FORCE_PATH_STYLE:-1}
    depends_on:
      datowave-turn:
        condition: service_started
      datowave-db-prod:
        condition: service_healthy
      datowave-redis-prod:
        condition: service_healthy
    networks:
      - datowave_public
      - data_prod

  datowave-db-prod:
    image: postgres:16
    container_name: datowave-db-prod
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${PROD_POSTGRES_DB}
      POSTGRES_USER: ${PROD_POSTGRES_USER}
      POSTGRES_PASSWORD: ${PROD_POSTGRES_PASSWORD}
    volumes:
      - ${POSTGRES_PROD_DATA_DIR:-/Volumes/datas3/datowave/postgres-db/prod}:/var/lib/postgresql/data
      - ../infra/postgres/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U ${PROD_POSTGRES_USER} -d ${PROD_POSTGRES_DB}"
        ]
      interval: 10s
      timeout: 5s
      retries: 10
    networks:
      - data_prod

  datowave-redis-prod:
    image: redis:7-alpine
    container_name: datowave-redis-prod
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 10
    volumes:
      - ${REDIS_PROD_DATA_DIR:-/Volumes/datas3/datowave/redis/prod}:/data
    networks:
      - data_prod

  datowave-minio-prod:
    image: minio/minio:latest
    container_name: datowave-minio-prod
    restart: unless-stopped
    profiles: ["minio-prod"]
    environment:
      MINIO_ROOT_USER: ${PROD_MINIO_ROOT_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${PROD_MINIO_ROOT_PASSWORD:-minioadmin}
    command: server /data --console-address ":9001"
    ports:
      - "${PROD_MINIO_API_PORT:-29000}:9000"
      - "${PROD_MINIO_CONSOLE_PORT:-29001}:9001"
    volumes:
      - ${MINIO_PROD_DATA_DIR:-/Volumes/datas3/datowave/minio-prod}:/data
    networks:
      - data_prod

  datowave-minio-prod-init:
    image: minio/mc:latest
    container_name: datowave-minio-prod-init
    restart: "no"
    profiles: ["minio-prod"]
    depends_on:
      datowave-minio-prod:
        condition: service_started
    entrypoint:
      - /bin/sh
      - -ec
    command:
      - |
        BUCKET="${PROD_MINIO_BUCKET:-chat-attachments-prod}"
        API_USER="${PROD_MINIO_API_USER:-${PROD_CHAT_MINIO_ACCESS_KEY:-}}"
        API_PASSWORD="${PROD_MINIO_API_PASSWORD:-${PROD_CHAT_MINIO_SECRET_KEY:-}}"

        if [ -z "$${API_USER}" ] || [ -z "$${API_PASSWORD}" ]; then
          API_USER="${PROD_MINIO_ROOT_USER:-minioadmin}"
          API_PASSWORD="${PROD_MINIO_ROOT_PASSWORD:-minioadmin}"
          echo "[minio-prod-init] warning: PROD_MINIO_API_USER/PASSWORD are not set; fallback to root credentials" >&2
        fi

        attempt=1
        until mc alias set local http://datowave-minio-prod:9000 "${PROD_MINIO_ROOT_USER:-minioadmin}" "${PROD_MINIO_ROOT_PASSWORD:-minioadmin}"; do
          if [ "$$attempt" -ge 30 ]; then
            echo "[minio-prod-init] failed to reach MinIO after $${attempt} attempts" >&2
            exit 1
          fi
          attempt=$$((attempt + 1))
          sleep 2
        done

        mc mb --ignore-existing "local/$${BUCKET}"
        mc anonymous set none "local/$${BUCKET}"

        mc ilm rule add --expire-delete-marker "local/$${BUCKET}" >/dev/null 2>&1 || true

        cat >/tmp/chat-attachments-rw-policy.json <<EOF
        {
          "Version": "2012-10-17",
          "Statement": [
            {
              "Effect": "Allow",
              "Action": [
                "s3:ListBucket"
              ],
              "Resource": [
                "arn:aws:s3:::$${BUCKET}"
              ]
            },
            {
              "Effect": "Allow",
              "Action": [
                "s3:GetObject",
                "s3:PutObject",
                "s3:DeleteObject"
              ],
              "Resource": [
                "arn:aws:s3:::$${BUCKET}/*"
              ]
            }
          ]
        }
        EOF

        mc admin policy create local chat-attachments-rw /tmp/chat-attachments-rw-policy.json >/dev/null 2>&1 || true
        mc admin user add local "$${API_USER}" "$${API_PASSWORD}" >/dev/null 2>&1 || true
        mc admin policy attach local chat-attachments-rw --user "$${API_USER}" >/dev/null 2>&1 || true
    networks:
      - data_prod

  datowave-livekit-prod:
    image: livekit/livekit-server:v1.9.12
    container_name: datowave-livekit-prod
    restart: unless-stopped
    profiles: ["livekit-prod"]
    environment:
      LIVEKIT_PROD_API_KEY: ${PROD_LIVEKIT_API_KEY:-}
      LIVEKIT_PROD_API_SECRET: ${PROD_LIVEKIT_API_SECRET:-}
      LIVEKIT_PROD_SIGNAL_PORT: ${PROD_LIVEKIT_SIGNAL_PORT:-7880}
      LIVEKIT_PROD_TCP_PORT: ${PROD_LIVEKIT_TCP_PORT:-7882}
      LIVEKIT_PROD_RTC_PORT_START: ${PROD_LIVEKIT_RTC_PORT_START:-35000}
      LIVEKIT_PROD_RTC_PORT_END: ${PROD_LIVEKIT_RTC_PORT_END:-35999}
    entrypoint:
      - /bin/sh
      - -ec
    command:
      - |
        if [ -z "$${LIVEKIT_PROD_API_KEY:-}" ] || [ -z "$${LIVEKIT_PROD_API_SECRET:-}" ]; then
          echo "[livekit-prod] PROD_LIVEKIT_API_KEY and PROD_LIVEKIT_API_SECRET must be set" >&2
          exit 1
        fi
        cat > /tmp/livekit.yaml <<EOF
        port: ${PROD_LIVEKIT_SIGNAL_PORT:-7880}
        bind_addresses:
          - "0.0.0.0"
        rtc:
          tcp_port: ${PROD_LIVEKIT_TCP_PORT:-7882}
          port_range_start: ${PROD_LIVEKIT_RTC_PORT_START:-35000}
          port_range_end: ${PROD_LIVEKIT_RTC_PORT_END:-35999}
          use_external_ip: true
        redis:
          address: datowave-redis-prod:6379
        logging:
          level: info
        EOF
        exec /livekit-server --config /tmp/livekit.yaml --keys "$${LIVEKIT_PROD_API_KEY}: $${LIVEKIT_PROD_API_SECRET}"
    ports:
      - "${PROD_LIVEKIT_TCP_PORT:-7882}:${PROD_LIVEKIT_TCP_PORT:-7882}/tcp"
      - "${PROD_LIVEKIT_RTC_PORT_START:-35000}-${PROD_LIVEKIT_RTC_PORT_END:-35999}:${PROD_LIVEKIT_RTC_PORT_START:-35000}-${PROD_LIVEKIT_RTC_PORT_END:-35999}/udp"
    depends_on:
      datowave-redis-prod:
        condition: service_healthy
    networks:
      - datowave_public
      - data_prod

networks:
  datowave_public:
    name: datowave_public
    external: true
  data_test:
    driver: bridge
  data_prod:
    driver: bridge
