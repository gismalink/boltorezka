# 2026-03-07

## SFU Stage 1 closure + Stage 2 canary kickoff (test)

### Delivered

- В `scripts/deploy/postdeploy-smoke-test.sh` baseline realtime gate адаптирован для Stage 1:
  - при коллизии baseline room и `SMOKE_SFU_ROOM_SLUG` baseline topology-assert отключается,
  - topology строго проверяется отдельным SFU pass.
- В `scripts/smoke/smoke-realtime.mjs` улучшена диагностика:
  - `room.join` теперь обрабатывается как `ack|nack`,
  - при отказе выводится код причины (`RoomNotFound`, и т.п.).
- Stage 2 canary control добавлен в API:
  - `RTC_MEDIA_TOPOLOGY_SFU_USERS` в `apps/api/src/config.ts`/`config.types.ts`,
  - user-aware topology resolver и per-user `rooms.presence` в `apps/api/src/routes/realtime.ts`.
- Env wiring обновлен:
  - `infra/docker-compose.host.yml` (test/prod passthrough `RTC_MEDIA_TOPOLOGY_SFU_USERS`),
  - `infra/.env.host.example` (новые Stage 2 переменные).
- Добавлен runbook: `docs/runbooks/SFU_STAGE2_CANARY_RUNBOOK.md`.

### Validation

- `npm run check:api-types` — PASS.
- Stage 1 dark-launch validation (`test`) на ref `03918b9087f73349d0a5f0c328e114ec1cc5971f` — PASS:
  - baseline realtime pass,
  - SFU topology pass (`expectedMediaTopology=sfu`, `mediaTopologyFirstOk=true`).
- Stage 2 canary-user validation (`test`) на ref `cc9f7528adfac4e31db0aabba3fd72d9590be4ce` — PASS:
  - overrides:
    - `TEST_RTC_MEDIA_TOPOLOGY_DEFAULT=p2p`
    - `TEST_RTC_MEDIA_TOPOLOGY_SFU_ROOMS=`
    - `TEST_RTC_MEDIA_TOPOLOGY_SFU_USERS=667833a3-e2b7-4edf-9975-aaaef20547ae`
    - `SMOKE_EXPECT_MEDIA_TOPOLOGY=sfu`
  - результат: `deploy:test:smoke` done, `mediaTopologyFirstOk=true`, `reconnectOk=true`.

### Decision

- Stage 1 считается закрытым в `test`.
- Stage 2 (user canary) официально стартовал в `test` с test-first политикой и без изменений в `prod`.

## SFU Stage 3 kickoff: default SFU profile in test

### Delivered

- Добавлен Stage 3 rollout shortcut:
  - `scripts/deploy/deploy-test-sfu-default.sh`
  - `npm run deploy:test:sfu`
- Добавлен runbook `docs/runbooks/SFU_STAGE3_DEFAULT_SFU_TEST_RUNBOOK.md`.
- `docs/plans/SFU_MIGRATION_PLAN.md` обновлен: Stage 3 in progress, execution reference расширен.

### Validation

- `bash -n scripts/deploy/deploy-test-sfu-default.sh` — PASS.
- Stage 3 profile validation (`test`) на ref `f9c876c3fdd9beac6f5af6415c16275a355da465` — PASS:
  - overrides:
    - `TEST_RTC_MEDIA_TOPOLOGY_DEFAULT=sfu`
    - `TEST_RTC_MEDIA_TOPOLOGY_SFU_ROOMS=`
    - `TEST_RTC_MEDIA_TOPOLOGY_SFU_USERS=`
    - `SMOKE_EXPECT_MEDIA_TOPOLOGY=sfu`
  - результат: `deploy:test:smoke` done, `mediaTopologyFirstOk=true`, `reconnectOk=true`.

### Decision

- Для `test` принимаем направление `SFU-by-default`.
- P2P сохраняется как rollback path, а не как целевая topology.

## SFU Stage 3 continuation: rollout with stall-flap fix

### Delivered

- В `apps/web/src/hooks/rtc/voiceCallPeerRecovery.ts` снижены false-positive stalled transitions:
  - stalled теперь накапливается только при `isRemoteSpeaking=true`.
- В `apps/web/src/hooks/rtc/voiceCallConfig.ts` повышен порог детекта stalled:
  - `RTC_INBOUND_STALL_TICKS: 3 -> 5`.
- Изменения выкачены в `test` на ref `0fcad1454eca8dcbf1e91f3a787ec29d4c919386`.

### Validation

- `npm --prefix apps/web run -s build` — PASS.
- `deploy:test:sfu` на ref `0fcad1454eca8dcbf1e91f3a787ec29d4c919386` — 3 подряд PASS:
  - run #1: PASS,
  - run #2: PASS,
  - run #3: PASS.
- Во всех трех прогонах realtime smoke:
  - `expectedMediaTopology=sfu`,
  - `mediaTopologyFirstOk=true`,
  - `reconnectOk=true`.

### Decision

- Текущий Stage 3 профиль (`SFU-by-default` в `test`) считается стабильным на серии из 3 подряд smoke-проходов.

## SFU Stage 4 readiness package (pre-prod, no-go)

### Delivered

- Добавлен `docs/runbooks/SFU_STAGE4_PROD_READINESS_PACKAGE.md`.
- Обновлены pre-prod документы:
  - `docs/runbooks/PREPROD_CHECKLIST.md` (SFU stage gate section),
  - `docs/runbooks/PREPROD_DECISION_PACKAGE.md` (refresh 2026-03-07).

### Validation

- Source evidence imported from server artifacts:
  - `.deploy/last-smoke-summary.env`
  - `.deploy/release-log.tsv`
  - `~/srv/edge/RELEASE_LOG.md`
- Stage 3 candidate `0fcad14` закреплен как `PASS x3` в SFU-default test profile.

### Decision

- Stage 4 package подготовлен, но финальный статус сохранен `NO-GO` до merge-to-main и explicit prod approval.

## SFU Stage 3 continuation: post-UI rollout evidence refresh

### Delivered

- После UI-обновлений выполнен очередной `deploy:test:sfu` на ref `4f9655ed6c9c35187ad7d348d6732e72c8c5678b`.

### Validation

- Postdeploy summary (`.deploy/last-smoke-summary.env`) — `SMOKE_STATUS=pass`.
- Release logs (`.deploy/release-log.tsv`, `~/srv/edge/RELEASE_LOG.md`) зафиксировали `rollout test pass` для `sha=4f9655e`.
- Из deploy-лога `/tmp/deploy-sfu.log` подтвержден Stage 3 topology gate:
  - `expectedMediaTopology: sfu`
  - `mediaTopologyFirstOk: true`
- Цепочка Stage 3 SFU-default rollout обновлена до `PASS x4 consecutive`.

### Decision

- Stage 3 остается в статусе стабильного `SFU-by-default` профиля в `test`.
- Статус pre-prod не меняется: `NO-GO` до merge в `main` и явного prod approval.
