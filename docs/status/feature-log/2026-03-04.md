# Boltorezka Feature Log

## 2026-03-04

## 2026-03-04 — Web video runtime and server controls (sender-side effects + ASCII)

### Delivered

- Реализован sender-side video effects pipeline для outgoing camera track:
  - `none` (raw),
  - `pixel8` (8-bit quantization + dithering + optional grid),
  - `ascii` (glyph-based luminance rendering).
- Добавлен server-level selector типа эффекта в `Server -> Video` (`dropdown`) и условный рендер соответствующих настроек.
- Добавлены и интегрированы настройки `ASCII`:
  - glyph size,
  - contrast,
  - font color.
- Добавлен owner preview в начале Server Video tab; preview использует те же параметры обработки, что и outgoing pipeline.
- Video windows overlay доработан:
  - drag за любую область окна,
  - resize за любой угол,
  - resize handles видимы только при hover/focus,
  - local/self tile mirrored по X.
- Server controls расширены для ресайза окон:
  - min width,
  - max width (увеличен до `480`).

### Validation

- Повторные локальные `apps/web -> npm run build` — PASS на каждом инкременте.
- Feature test rollouts выполнены последовательно и зафиксированы smoke PASS:
  - `1dea7b3` — owner preview + sender-side pipeline,
  - `e16dacd` — video window UX + pixel-grid control,
  - `fd93196` — ASCII effect + effect type selector,
  - `38588f4` — server controls: resize bounds + ASCII color,
  - `1c40a14` — compact sliders layout + max resize width `480`.
- Последний зафиксированный прогон:
  - `ssh mac-mini 'cd ~/srv/boltorezka && TEST_REF=origin/feature/video-stream-overlay-chat-toggle npm run deploy:test:smoke'` — PASS (`health`, `smoke:sso`, `smoke:api`, `smoke:realtime`).

### Notes

- Для визуальной консистентности receiver-side CSS pixel overlay отключён; эффект применяется на стороне отправителя.
- Test-first / feature-branch workflow соблюдён на всех инкрементах.

## 2026-03-04 — Frontend version-cache gate + dual-path static readiness

### Delivered

- Введён version/cache compatibility gate:
  - API: `GET /version` (`appVersion`, `appBuildSha`),
  - web: периодическая проверка версии и auto-reload при несовпадении build SHA,
  - cache policy: `index.html` no-store/no-cache, hash-assets immutable.
- Build SHA прокидывается единообразно в:
  - frontend bundle (`VITE_APP_VERSION`),
  - server runtime (`APP_BUILD_SHA`) через deploy scripts.
- Добавлен отдельный static delivery path для test readiness: `/{__web}/`.
- Добавлен и включён в postdeploy gate smoke `smoke:web:version-cache`.

### Validation

- `TEST_REF=origin/feature/video-stream-overlay-chat-toggle npm run deploy:test:smoke` — PASS на `edb033f`.
- Split-path dual validation в test:
  - `SMOKE_API_URL=https://test.boltorezka.gismalink.art SMOKE_WEB_BASE_URL=https://test.boltorezka.gismalink.art/__web npm run smoke:web:static` — PASS.
  - `SMOKE_API_URL=https://test.boltorezka.gismalink.art SMOKE_WEB_BASE_URL=https://test.boltorezka.gismalink.art/__web SMOKE_EXPECT_BUILD_SHA=edb033fa61aaeb71df24f78d3055b8c3f1c49f1d npm run smoke:web:version-cache` — PASS.

### Decision

- Dual-path readiness validation (separate static delivery path in test) переведён в **DONE**.

## 2026-03-04 — Runtime auto-update of input device on system devicechange

### Delivered

- Для active call добавлена runtime-реакция на `navigator.mediaDevices.devicechange` в media-effects layer.
- При system switch устройства ввода выполняется авто-refresh исходящего audio track:
  - работает для explicit device id,
  - работает для `selectedInputId=default` (частый кейс macOS auto-route при смене наушников).
- В call log добавлены явные записи о success/failure авто-обновления input track.

### Validation

- `npm run web:build` — PASS.
- `npm run check:api-types` — PASS.

### Decision

- Пункт roadmap по авто-обновлению input device при system devicechange переведён в **DONE**.

## 2026-03-04 — Post-MVP performance gate formalization

### Delivered

- Зафиксированы пороги GO/NO-GO для post-MVP performance gate:
  - API latency thresholds (p95/p99 + timeout rate),
  - realtime reconnect/call relay success,
  - version-cache compatibility gate,
  - operational stability constraints.
- Добавлен канонический документ: `docs/operations/PERFORMANCE_GATE.md`.

### Decision

- Пункт roadmap `Зафиксировать post-MVP performance gate` переведён в **DONE**.

## 2026-03-04 — Pre-prod decision package refresh (gate sync)

### Delivered

- Обновлён `docs/runbooks/PREPROD_DECISION_PACKAGE.md` под актуальный цикл evidence:
  - latest verified test deploy `b931324`,
  - включён `smoke:web:version-cache` в обязательный GO контур,
  - зафиксирована привязка к `docs/operations/PERFORMANCE_GATE.md`.
- Актуализирован текущий gate record в пакете для нового pre-prod решения (поля owner/rollback оставлены к заполнению перед prod).

### Decision

- Пункт roadmap `Выполнить новый pre-prod package refresh` переведён в **DONE**.
- Итог состояния пакета: **NO-GO до заполнения sign-off полей и explicit prod approval**.

## 2026-03-04 — Browser-level denied-media E2E (headless)

### Delivered

- Добавлен browser-level headless smoke для denied-media UX: `scripts/smoke-web-denied-media-browser.mjs`.
- Добавлен npm script: `smoke:web:denied-media:browser`.
- В `scripts/smoke-web-e2e.sh` добавлен optional stage:
  - toggle: `SMOKE_E2E_DENIED_MEDIA_BROWSER=0|1` (default `0`),
  - при включении выполняется browser-level проверка denied banner + request access CTA.
- Обновлена smoke matrix: `docs/operations/SMOKE_CI_MATRIX.md`.

### Decision

- Пункт roadmap `Browser-level E2E: denied media permissions UX` переведён в **DONE**.
- Stage сделан opt-in, чтобы сохранить стабильность текущего postdeploy gate на server контуре.

## 2026-03-04 — Phase 0 package formalization (MVP boundaries + ADR)

### Delivered

- Добавлен канонический пакет `docs/architecture/PHASE0_MVP_ADR.md`.
- В пакете зафиксированы:
  - MVP boundaries (participants/retention/platforms),
  - ADR summary по signaling/media topology/auth-session,
  - canonical references на архитектуру, voice baseline и pre-prod gate.

### Decision

- Пункты roadmap `Phase 0 — Discovery & ADR` переведены в **DONE**.

## 2026-03-04 — Phase 3 policy formalization (Voice / WebRTC MVP)

### Delivered

- Добавлен канонический policy-документ `docs/runbooks/PHASE3_VOICE_WEBRTC_MVP_POLICY.md`.
- В документе формализованы:
  - room-size limit policy для MVP (с привязкой к validated test/load evidence),
  - graceful degradation policy (runtime + operational guardrails),
  - mapping всех пунктов `Phase 3` к статусу completion.

### Decision

- Пункты roadmap `Phase 3 — Voice / WebRTC MVP` переведены в **DONE**.

## 2026-03-04 — Roadmap consistency sweep (Phase 4 parent tasks)

### Delivered

- В `docs/status/ROADMAP.md` синхронизированы parent-checkboxes Phase 4 с уже закрытыми подпунктами:
  - `E2E smoke сценарии`,
  - `карточка пользователя`,
  - `Discord-like структура каналов`.

### Decision

- Устранена несогласованность roadmap (верхний уровень теперь соответствует фактическому completion подпунктов).

## 2026-03-04 — Roadmap consistency sweep (focus + next execution batch)

### Delivered

- В `docs/status/ROADMAP.md` актуализированы:
  - `Focus now` (смещён на реально открытые Phase 5/6 workstreams),
  - `Execution plan` (добавлен следующий batch open-этапов после полностью закрытых пунктов 1-4).

### Decision

- Roadmap отражает не только completed history, но и ближайший фактический фронт работ без пустого execution tail.

## 2026-03-04 — Phase 5 scope note formalization (iOS/macOS kickoff)

### Delivered

- Добавлен канонический planning-документ: `docs/plans/PHASE5_IOS_MACOS_SCOPE_NOTE.md`.
- В документе зафиксированы:
  - MVP boundaries для Phase 5 handoff (in-scope / out-of-scope),
  - shared Swift package bootstrap contract (`CoreModels`, `Networking`, `Realtime`, `CallEngine`),
  - kickoff acceptance criteria и ссылки на канонические зависимости.
- В roadmap закрыт execution пункт #5 (`Phase 5 scope note`).

### Decision

- Phase 5 execution остаётся в статусе **open** (реализация задач не начата), но kickoff scope и bootstrap-контракт формально зафиксированы.

## 2026-03-04 — Legacy public deprecation plan activated (execution focus)

### Delivered

- В `docs/status/ROADMAP.md` добавлен активный workstream по `docs/runbooks/LEGACY_PUBLIC_DEPRECATION_PLAN.md`.
- В open execution items добавлены оставшиеся задачи Phase D (decommission cleanup/rehearsal/runbook finalization).
- `docs/status/STATUS.md` синхронизирован: deprecation plan отмечен как текущий execution focus.

### Decision

- План deprecation переведён в активную фазу исполнения; закрытые этапы A/B/C не дублируются в roadmap, фокус только на оставшихся open-шагах.

## 2026-03-04 — Legacy deprecation Phase D: API/static runtime decoupling toggle

### Delivered

- В `apps/api/src/config.ts` добавлен флаг `API_SERVE_STATIC` (boolean parser, default `true`).
- В `apps/api/src/index.ts` static routes (`/`, `/__web/`) регистрируются только при `API_SERVE_STATIC=1`.
- В `infra/docker-compose.host.yml` для server окружений добавлено отключение static в API:
  - `TEST_API_SERVE_STATIC=0`,
  - `PROD_API_SERVE_STATIC=0`.
- В `infra/.env.host.example` добавлены соответствующие env-переменные.

### Decision

- Первый open-пункт Phase D (`decommission change-set`) переведён в **DONE**.
- Следующий шаг: test rehearsal decommission + rollback evidence.

## 2026-03-04 — Legacy deprecation Phase D: test rehearsal + rollback evidence

### Delivered

- Выполнен test rehearsal rollout на `origin/feature/video-stream-overlay-chat-toggle` (`8eeed7b`).
- Результат rehearsal:
  - deploy + `smoke:sso` + `smoke:api` — PASS,
  - `smoke:web:version-cache` — FAIL (`index fetch failed: 404`) после отключения API static serving.
- Выполнен rollback rehearsal на `b931324`:
  - `TEST_REF=b931324 npm run deploy:test:smoke` — PASS.
- В `scripts/examples/postdeploy-smoke-test.sh` добавлена поддержка отдельного web base URL (`SMOKE_WEB_BASE_URL`) независимо от API base.

### Decision

- Пункт roadmap `Phase D rehearsal + rollback evidence` переведён в **DONE**.
- Финальный пункт deprecation остаётся open до выделения отдельного внешнего static delivery path и его smoke-валидации.

## 2026-03-04 — Legacy deprecation Phase D: external static path change-set

### Delivered

- Добавлен отдельный web static image/runtime:
  - `apps/web/Dockerfile.static`,
  - `apps/web/nginx.static.conf` (SPA fallback + cache policy для `index.html` и hash-assets).
- В `infra/docker-compose.host.yml` добавлены сервисы:
  - `boltorezka-web-test`,
  - `boltorezka-web-prod`.
- Deploy scripts переведены на rollout `api+web`:
  - `scripts/examples/deploy-test-from-ref.sh`,
  - `scripts/examples/deploy-prod-from-ref.sh`.
- Ingress routing change-set подготовлен в `edge/ingress/caddy/Caddyfile`:
  - API path matcher (`/v1*`, `/health`, `/version`, `/metrics`) -> API,
  - default path + `/__web/*` -> web static service.

### Decision

- Внешний static delivery path реализован как инфраструктурный change-set; финальный roadmap пункт остаётся open до server-side rollout и smoke PASS на test.
