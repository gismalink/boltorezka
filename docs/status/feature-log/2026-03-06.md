# Boltorezka Feature Log

## 2026-03-06

## 2026-03-06 — RTC Phase 1: canonical media-state + initial replay

### Delivered

- В `apps/api/src/routes/realtime.ts` добавлен canonical media-state store на ключе `roomId:userId` для `muted/speaking/audioMuted/localVideoEnabled`.
- На `room.join` добавлен snapshot replay `call.initial_state` до обычных realtime дельт.
- Canonical media-state обновляется при `call.mic_state` и `call.video_state`, очищается при leave/kick/evict/disconnect.
- В `apps/api/src/ws-protocol.types.ts` и `apps/api/src/ws-protocol.ts` добавлены типы и envelope builder для `call.initial_state`.
- В `apps/web/src/services/wsMessageController.ts` добавлен обработчик `call.initial_state` с replay в существующие mic/video callbacks.
- В `apps/web/src/hooks/useRealtimeChatLifecycle.ts` прокинут `onCallInitialState` в message controller.
- В `apps/web/src/App.tsx` и `apps/web/src/hooks/useVoiceRoomStateMaps.ts` добавлен first-render seed maps merge для детерминированных mic/camera/audio-muted индикаторов.

### Validation

- `npm --prefix apps/web run -s build` — PASS.
- `npm --prefix apps/api run -s typecheck` — PASS.

### Decision

- Phase 1 (`state consistency foundation`) переведен в DONE.
- Следующий активный этап: Phase 2 (`negotiation reliability`) в той же feature-ветке, test-first.

## 2026-03-06 — RTC Phase 2: negotiation manager + fairness queue

### Delivered

- В `apps/web/src/hooks/rtc/voiceCallNegotiationState.ts` добавлен централизованный queue-state manager:
	- `createOfferQueueState`,
	- `enqueueOfferRequest` (dedupe + bucket priority),
	- `dequeueNextOfferRequest` (fair round-robin by bucket),
	- `markOfferQueueActiveForTarget`,
	- `clearOfferQueue`.
- В `apps/web/src/hooks/rtc/voiceCallOfferPolicy.ts` добавлены retry policy primitives:
	- `resolveOfferRetryBudget`,
	- `resolveOfferRetryDelayMs`,
	- bucket-specific budget/delay для `manual|ice-restart|video-sync`.
- В `apps/web/src/hooks/rtc/useVoiceCallRuntime.ts` `startOffer` переведён на queue-first flow:
	- enqueue вместо прямого выполнения,
	- single drain loop,
	- retry по budget при preflight/signal failures,
	- сохранена текущая `none` default-политика для video effects.

### Validation

- `npm --prefix apps/web run -s build` — PASS.

### Decision

- Phase 2 (`negotiation reliability`) переведен в DONE.
- Следующий активный этап: `#6.3` (observability + smoke/deploy gates).

## 2026-03-06 — RTC #6.3 increment: initial-state observability + gate hardening

### Delivered

- В `apps/api/src/routes/realtime.ts` добавлены метрики replay-path:
	- `call_initial_state_sent`,
	- `call_initial_state_participants_total`.
- В `scripts/smoke/smoke-realtime.mjs` добавлены строгие проверки `call.initial_state` replay после `room.join`:
	- в базовом join,
	- во втором join call-signal сценария,
	- флаг `SMOKE_REQUIRE_INITIAL_STATE_REPLAY=1` (default-on).
- В `scripts/deploy/postdeploy-smoke-test.sh` усилен gate:
	- realtime smoke запускается с `SMOKE_REQUIRE_INITIAL_STATE_REPLAY=1`,
	- в summary добавлены delta-поля `initial_state` и `initial_state_participants`.

### Validation

- `node --check scripts/smoke/smoke-realtime.mjs` — PASS.
- `bash -n scripts/deploy/postdeploy-smoke-test.sh` — PASS.
- `npm --prefix apps/api run -s typecheck` — PASS.
- `npm --prefix apps/web run -s build` — PASS.

### Decision

- `#6.3` переведен в partial-progress: replay observability и `call.initial_state` postdeploy gate уже внедрены.
- Следующий шаг по `#6.3`: дополнить media-browser smoke camera convergence assertion и обновить SLO/runbook docs.

## 2026-03-06 — RTC #6.3 docs/workflow sync

### Delivered

- В `.github/workflows/test-smoke.yml` добавлен optional extended RTC path:
	- `workflow_dispatch` input `extended_rtc`,
	- repo var fallback `TEST_SMOKE_EXTENDED_RTC`,
	- optional secrets `TEST_SMOKE_TEST_BEARER_TOKEN_SECOND/THIRD`,
	- replay requirement `SMOKE_REQUIRE_INITIAL_STATE_REPLAY=1` в default CI path.
- Обновлена матрица `docs/operations/SMOKE_CI_MATRIX.md`:
	- контракт `call.initial_state` replay,
	- переменные/секреты для extended CI,
	- явная фиксация replay gate в postdeploy/CI.
- Обновлён `docs/runbooks/VOICE_BASELINE_RUNBOOK.md`:
	- observability signals для replay-path,
	- triage чеклист для late-join regressions.

### Decision

- Phase 3 documentation task закрыт.
- Phase 4 workflow wiring task закрыт.
- В `#6.3` остается последний technical item: camera convergence assertions в media-browser smoke.

## 2026-03-06 — RTC #6.3 final technical item: camera convergence smoke

### Delivered

- В `scripts/smoke/smoke-realtime-media-browser.mjs` добавлен capture `call.video_state` событий по `fromUserId`.
- Добавлены runtime helpers в browser-smoke странице:
	- `sendLocalVideoState(enabled)`,
	- `waitForRemoteVideoState(fromUserId, expected)`.
- Добавлены двусторонние convergence assertions:
	- A -> B: `false -> true`,
	- B -> A: `false -> true`.
- В итоговый smoke JSON добавлены:
	- `cameraStateConvergenceOk`,
	- `remoteVideoStateA`,
	- `remoteVideoStateB`.

### Validation

- `node --check scripts/smoke/smoke-realtime-media-browser.mjs` — PASS.
- `node --check scripts/smoke/smoke-realtime.mjs` — PASS.
- `bash -n scripts/deploy/postdeploy-smoke-test.sh` — PASS.
- `npm --prefix apps/api run -s typecheck` — PASS.
- `npm --prefix apps/web run -s build` — PASS.

### Decision

- Phase 4 tasks в `RTC_STABILITY_ROADMAP.md` переведены в DONE.
- Открытый хвост `#6.3`: расширить server-side RTC metric set до `offer/glare/reconnect/state-lag` (сейчас закрыт replay-path baseline).

## 2026-03-06 — RTC #6.3 closure: server-side metrics expansion

### Delivered

- В `apps/api/src/routes/realtime.ts` расширен server-side RTC metrics set:
	- ingress: `call_offer_received`, `call_answer_received`, `call_ice_received`,
	- glare proxy: `call_glare_suspected` (встречные offer в коротком окне),
	- reliability: `call_offer_rate_limited`, target-miss counters (`call_*_target_miss`),
	- reconnect: `call_reconnect_joined` (join в reconnect window),
	- replay lag: `call_initial_state_lag_ms_total`, `call_initial_state_lag_samples`.

### Validation

- `npm --prefix apps/api run -s typecheck` — PASS.
- `npm --prefix apps/web run -s build` — PASS.

### Decision

- `#6.3` полностью закрыт.
- Следующий открытый фронт: Phase 5 feature flags + controlled rollout strategy.

## 2026-03-06 — RTC Phase 5 increment: controlled rollout feature flags

### Delivered

- В `apps/api/src/config.types.ts` и `apps/api/src/config.ts` добавлен server toggle:
	- `RTC_FEATURE_INITIAL_STATE_REPLAY` (`config.rtcFeatureInitialStateReplay`).
- В `apps/api/src/routes/realtime.ts` отправка `call.initial_state` и replay-метрики на `room.join` теперь условные по `RTC_FEATURE_INITIAL_STATE_REPLAY`.
- В `apps/web/src/hooks/rtc/voiceCallConfig.ts` добавлены web toggles:
	- `VITE_RTC_FEATURE_INITIAL_STATE_REPLAY`,
	- `VITE_RTC_FEATURE_NEGOTIATION_MANAGER_V2`,
	- `VITE_RTC_FEATURE_OFFER_QUEUE`.
- В `apps/web/src/services/wsMessageController.ts` `call.initial_state` replay обрабатывается только при включенном `VITE_RTC_FEATURE_INITIAL_STATE_REPLAY`.
- В `apps/web/src/hooks/rtc/useVoiceCallRuntime.ts` добавены rollback-переключатели для offer flow:
	- `VITE_RTC_FEATURE_OFFER_QUEUE=0` переводит `startOffer` в direct path без queue/retry.
	- `VITE_RTC_FEATURE_NEGOTIATION_MANAGER_V2=0` ослабляет manager-specific preflight path до legacy-friendly режима.

### Validation

- `npm --prefix apps/api run -s typecheck` — PASS.
- `npm --prefix apps/web run -s build` — PASS.

### Decision

- Первый пункт Phase 5 (`feature flags`) переведен в DONE.
- Следующий открытый шаг Phase 5: накопить стабильные метрики и собрать `SFU_MIGRATION_PLAN` decision package.

## 2026-03-06 — RTC Phase 5 closure: SFU migration decision package

### Delivered

- Добавлен canonical документ `docs/architecture/SFU_MIGRATION_PLAN.md`:
	- decision matrix (`P2P-only` vs `Hybrid` vs `SFU-only`),
	- выбранный путь: `Hybrid` (P2P default + SFU for larger rooms),
	- entry gates, migration stages, success criteria и rollback policy,
	- ownership и evidence references.
- `docs/status/RTC_STABILITY_ROADMAP.md` синхронизирован: второй пункт Phase 5 отмечен как выполненный.

### Decision

- Phase 5 (`controlled rollout`) закрыт полностью.
- Следующий фронт: acceptance gates и supporting hardening workstream (`#6`/`#7` в `docs/status/ROADMAP.md`).

## 2026-03-06 — Chat image UX + host-side DB backups

### Delivered

- В `apps/web/src/App.tsx` увеличены ограничения вставляемых изображений для чата:
	- `MAX_CHAT_IMAGE_MAX_SIDE` -> `1200`,
	- `MAX_CHAT_IMAGE_DATA_URL_LENGTH` увеличен для более крупных превью.
- В `apps/web/src/components/ChatPanel.tsx` добавлено:
	- кликабельное превью изображений в сообщениях,
	- popup-modal для просмотра изображения,
	- preview блока для вставленных (paste) изображений до отправки.
- В `apps/web/src/styles.css` добавлены стили для image-preview кнопок и popup.
- В `apps/web/src/i18n.ts` добавлены локализации для image preview действий.
- Добавлен host-side backup контур для Postgres вне Docker volume:
	- `scripts/ops/backup-postgres-all.sh` (`pg_dumpall` для test/prod + gzip + sha256 + retention),
	- `docs/operations/DB_BACKUP_RUNBOOK.md` с запуском и расписанием,
	- `package.json` script: `npm run backup:db:all`.

### Validation

- `npm --prefix apps/web run -s build` — PASS.
- `npm --prefix apps/api run -s typecheck` — PASS.

### Decision

- Базовая защита от потери Docker volume зафиксирована: резервные копии вынесены на host FS.
- UX вставки изображений в чат улучшен без изменения WS/DB контрактов.

## 2026-03-06 — Chat image compose UX: hidden payload + compact preview

### Delivered

- В `apps/web/src/App.tsx` вставка изображения переведена на `pending image` state:
	- base64 payload больше не вставляется в поле ввода,
	- изображение отправляется как markdown только в момент `chat.send`.
- В `apps/web/src/components/ChatPanel.tsx` preview в compose оставлен компактным и отображается справа от поля ввода.
- В `apps/web/src/i18n.ts` уточнен toast-текст `chat.imageTooLarge` (причина после сжатия).

### Validation

- `npm --prefix apps/web run -s build` — PASS.

### Decision

- Убрана деградация UX с длинным base64 в input при paste image.

## 2026-03-06 — SFU migration kickoff: Stage 0 started

### Delivered

- Актуализирован и переведен на русский canonical документ `docs/architecture/PHASE0_MVP_ADR.md`.
- В `docs/architecture/SFU_MIGRATION_PLAN.md` статус переведен в execution с ссылкой на Stage 0.
- Добавлен `docs/architecture/SFU_STAGE0_EXECUTION_PLAN.md` с scope, выходными gate и задачами Stage 0.

### Decision

- Stage 0 официально стартовал в test-first режиме.

## 2026-03-06 — SFU Stage 0 scaffold: mediaTopology routing contract

### Delivered

- В `apps/api/src/config.ts`/`apps/api/src/config.types.ts` добавлены Stage 0 routing env hooks:
	- `RTC_MEDIA_TOPOLOGY_DEFAULT` (`p2p|sfu`, default `p2p`),
	- `RTC_MEDIA_TOPOLOGY_SFU_ROOMS` (csv roomSlug list).
- В `apps/api/src/ws-protocol.types.ts` и `apps/api/src/ws-protocol.ts` расширены payload контракты:
	- `room.joined.mediaTopology`,
	- `room.presence.mediaTopology`,
	- `rooms.presence.rooms[].mediaTopology`.
- В `apps/api/src/routes/realtime.ts` внедрен room-level resolver topology и прокидка в room events.
- В `apps/web/src/services/wsMessageController.ts` и `apps/web/src/hooks/useRealtimeChatLifecycle.ts` добавлен client-side прием topology metadata.
- Добавлен канонический контракт Stage 0: `docs/architecture/SFU_SESSION_CONTRACT.md`.

### Validation

- `npm --prefix apps/api run -s typecheck` — PASS.
- `npm --prefix apps/web run -s build` — PASS.

### Decision

- Stage 0 routing metadata contract считается внедренным (без изменения default P2P runtime behavior).

## 2026-03-06 — Stage 0 gate hardening: realtime smoke mediaTopology assertions

### Delivered

- В `scripts/smoke/smoke-realtime.mjs` добавлены обязательные проверки `mediaTopology`:
	- `room.joined.payload.mediaTopology`,
	- `room.presence.payload.mediaTopology`,
	- для первого и второго `room.join` сценариев.
- В `scripts/deploy/postdeploy-smoke-test.sh` realtime gate усилен:
	- `SMOKE_REQUIRE_MEDIA_TOPOLOGY=1`,
	- `SMOKE_EXPECT_MEDIA_TOPOLOGY` берется из `${RTC_MEDIA_TOPOLOGY_DEFAULT:-p2p}`.

### Validation

- `node --check scripts/smoke/smoke-realtime.mjs` — PASS.
- `bash -n scripts/deploy/postdeploy-smoke-test.sh` — PASS.

### Decision

- Stage 0 routing contract переведен в enforce-mode на уровне postdeploy smoke.

## 2026-03-06 — Stage 0 dual-topology smoke profile (p2p + optional sfu)

### Delivered

- В `scripts/deploy/postdeploy-smoke-test.sh` добавлен optional второй realtime smoke прогон для SFU-rooms:
	- включается при `SMOKE_SFU_ROOM_SLUG=<slug>`,
	- ожидаемая топология задается `SMOKE_SFU_EXPECT_MEDIA_TOPOLOGY` (default `sfu`).
- В smoke summary добавлен статус `sfu_topology`.

### Validation

- `bash -n scripts/deploy/postdeploy-smoke-test.sh` — PASS.

### Decision

- postdeploy gate теперь может проверять оба routing режима в одном прогоне без изменения default behavior.

## 2026-03-06 — Stage 0 wiring fix: compose env passthrough for media topology

### Delivered

- В `infra/docker-compose.host.yml` добавлен passthrough media topology env для API:
	- test: `TEST_RTC_MEDIA_TOPOLOGY_DEFAULT`, `TEST_RTC_MEDIA_TOPOLOGY_SFU_ROOMS`,
	- prod: `PROD_RTC_MEDIA_TOPOLOGY_DEFAULT`, `PROD_RTC_MEDIA_TOPOLOGY_SFU_ROOMS`.
- Исправлена причина false-negative при forced SFU smoke (переменные раньше не попадали в контейнер runtime).

### Validation

- One-off test deploy со значениями:
	- `TEST_RTC_MEDIA_TOPOLOGY_SFU_ROOMS=general`
	- `SMOKE_EXPECT_MEDIA_TOPOLOGY=sfu`
	- `SMOKE_SFU_ROOM_SLUG=general`
	- `SMOKE_SFU_EXPECT_MEDIA_TOPOLOGY=sfu`
- Результат: `mediaTopologyFirstOk=true`, `sfu_topology=pass`.

### Decision

- Stage 0 dual-topology smoke evidence подтверждено практически в test-контуре.
