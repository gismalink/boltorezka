# 2026-03-09

## LiveKit stabilization and RC confirmation (test)

### Delivered

- Frontend LiveKit runtime stabilized for `mediaTopology=livekit` rooms.
- Camera freeze mitigation applied in `apps/web/src/hooks/rtc/useLivekitVoiceRuntime.ts` by setting `adaptiveStream: false` for the current overlay rendering path.
- RC checkpoint already existed (`release-candidate-livekit-test-2026-03-09` on `63b4718`); additional fix deployed on top.

### Validation

- Test deploy command (server-side):
  - `ssh mac-mini 'cd ~/srv/boltorezka/apps && TEST_REF=origin/feature/scheduler-interface-portable npm run deploy:test:livekit'`
- Resolved/deployed SHA:
  - `694dc18cc62295389a6850da784b02b705efea3b`
- Postdeploy smoke:
  - `smoke:sso` - PASS
  - `smoke:api` - PASS
  - `smoke:web:version-cache` - PASS (`sha=694dc18...`)
  - `smoke:realtime` - PASS
  - `smoke:livekit:token-flow` - PASS
  - `smoke:livekit:media` - PASS (`oneWayIncidents.audio=0`, `oneWayIncidents.video=0`)

### User acceptance

- Manual confirmation from release owner in test: `все работает`.

### Decision

- `test` contour remains stable on LiveKit path with latest fix.
- `prod` remains blocked by policy until feature merge to `main` and explicit approval.