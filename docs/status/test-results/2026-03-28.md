# Test results — 2026-03-28

## Observability no-server gate + owner room creation (test, feature branch)

- Date: `2026-03-28`
- Branch: `origin/feature/multiserver-stage1-services`
- Applied SHA: `0b99f9fa36f1ffb8f7f2a28733cc61b8fb1fad6a`
- Scope:
  - вкладка `Наблюдаемость` скрыта/недоступна, если сервер не выбран;
  - guard для `server_management`/`observability` при отсутствии текущего сервера переводит на `desktop_downloads`;
  - права создания каналов (POST `/v1/rooms`) расширены: owner/admin текущего сервера могут создавать комнаты без global admin роли;
  - в web permissions `canCreateRooms` учитывает роль пользователя в текущем сервере (`owner/admin`) плюс `admin/super_admin`.
- Command (server):
  - `TEST_REF=origin/feature/multiserver-stage1-services SMOKE_MULTISERVER=1 SMOKE_MULTISERVER_AGE_GATE=1 npm run deploy:test:smoke`
- Result: `PASS` (`[deploy-test-smoke] done`).

Проверки (ключевые):
- `smoke:sso` -> `PASS`.
- `smoke:api` -> `PASS`.
- `smoke:auth:session` -> `PASS`.
- `smoke:auth:cookie-negative` -> `PASS`.
- `smoke:auth:cookie-ws-ticket` -> `PASS`.
- `smoke:web:version-cache` -> `PASS` (sha=`0b99f9f...`).
- `smoke:web:crash-boundary:browser` -> `PASS`.
- `smoke:web:rnnoise:browser` -> `PASS`.
- `smoke:desktop:update-feed` -> `PASS`.
- `smoke:multiserver` -> `PASS`.
- `smoke:multiserver:age-gate` -> `PASS` (`mode=no-room-skip`).
- `smoke:multiserver:role-matrix` -> `PASS` (`roles=admin,member,owner`).
- `smoke:realtime` -> `PASS` (`ok: true`).

Notes:
- Во время нескольких повторов были transient сетевые ошибки (`curl 000/timeout`, `localStorage Access is denied`) в browser-smoke; финальный прогон завершился штатно и полностью зеленым.

## Server profile access polish + onboarding copy (test, feature branch)

- Date: `2026-03-28`
- Branch: `origin/feature/multiserver-stage1-services`
- Applied SHA: `69cf6040ef45c9531377f40cb50f7a8b710b3338`
- Scope:
  - вкладка `Картинки чата` ограничена только `super_admin`;
  - из списка серверов убран дублирующий формат `name (slug)` -> только `name`;
  - в header убран fallback `// No server selected` (при пустом выборе показывается только `Dato`);
  - вкладка `Этот сервер` disabled при отсутствии выбранного сервера + guard fallback на `Наблюдаемость`;
  - onboarding при `0` серверах расширен до сценария "Приветствие и выбор": попросить invite или создать сервер.
- Command (server):
  - `TEST_REF=origin/feature/multiserver-stage1-services SMOKE_MULTISERVER=1 SMOKE_MULTISERVER_AGE_GATE=1 npm run deploy:test:smoke`
- Result: `PASS` (`[deploy-test-smoke] done`).

Проверки (ключевые):
- `smoke:sso` -> `PASS`.
- `smoke:api` -> `PASS`.
- `smoke:auth:session` -> `PASS`.
- `smoke:auth:cookie-negative` -> `PASS`.
- `smoke:auth:cookie-ws-ticket` -> `PASS`.
- `smoke:web:version-cache` -> `PASS` (sha=`69cf604...`).
- `smoke:web:crash-boundary:browser` -> `PASS`.
- `smoke:web:rnnoise:browser` -> `PASS`.
- `smoke:desktop:update-feed` -> `PASS`.
- `smoke:multiserver` -> `PASS`.
- `smoke:multiserver:age-gate` -> `PASS` (`mode=no-room-skip`).
- `smoke:multiserver:role-matrix` -> `PASS` (`roles=admin,member,owner`).
- `smoke:realtime` -> `PASS` (`ok: true`).

Notes:
- На health-check был кратковременный `502`, затем API автоматически восстановился и smoke завершился штатно.

## Server soft-delete endpoint + UI action wiring (test, feature branch)

- Date: `2026-03-28`
- Branch: `origin/feature/multiserver-stage1-services`
- Applied SHA: `918cd0f9635fc908a6345157d8a2ce988a830ab7`
- Scope:
  - добавлен backend endpoint `DELETE /v1/servers/:serverId` (soft-delete);
  - добавлена миграция `0009_servers_archive_soft_delete.sql` (`servers.is_archived` + индекс);
  - серверные выборки/middleware/admin/invite потоки учитывают `is_archived = FALSE`;
  - кнопка `Удалить сервер` в UI подключена к реальному API, с обработкой ошибок `forbidden_role` и `DefaultServerCannotBeDeleted`.
- Command (server):
  - `TEST_REF=origin/feature/multiserver-stage1-services SMOKE_MULTISERVER=1 SMOKE_MULTISERVER_AGE_GATE=1 npm run deploy:test:smoke`
- Result: `PASS` (`[deploy-test-smoke] done`).

Проверки (ключевые):
- `smoke:sso` -> `PASS`.
- `smoke:api` -> `PASS`.
- `smoke:auth:session` -> `PASS`.
- `smoke:auth:cookie-negative` -> `PASS`.
- `smoke:auth:cookie-ws-ticket` -> `PASS`.
- `smoke:web:version-cache` -> `PASS` (sha=`918cd0f...`).
- `smoke:web:crash-boundary:browser` -> `PASS`.
- `smoke:web:rnnoise:browser` -> `PASS`.
- `smoke:desktop:update-feed` -> `PASS`.
- `smoke:multiserver` -> `PASS`.
- `smoke:multiserver:age-gate` -> `PASS` (`mode=no-room-skip`).
- `smoke:multiserver:role-matrix` -> `PASS` (`roles=admin,member,owner`).
- `smoke:realtime` -> `PASS` (`ok: true`).

Notes:
- На health-check был кратковременный `502`, затем API автоматически восстановился и прогон завершился штатно.

## Server profile UX fixes (roles/badges/tabs/actions) (test, feature branch)

- Date: `2026-03-28`
- Branch: `origin/feature/multiserver-stage1-services`
- Applied SHA: `ea1cb3c67ad87d171e9e32ad6549d2f277a828ff`
- Scope:
  - скрытие default role в списках людей (`user`/`member` не показываются), роли выше отображаются badge;
  - переименование раздела `Наблюдаемость` (внутри: Лог/Сигналинг/Телеметрия);
  - фикс рендера `Управление продуктом -> Сервера` (табы и контент больше не пропадают);
  - в `Этот сервер` кнопки, связанные с полями, выровнены в одну строку;
  - `Этот сервер` открыт и для обычного пользователя; добавлены кнопки `Покинуть сервер` и `Удалить сервер` на странице.
- Command (server):
  - `TEST_REF=origin/feature/multiserver-stage1-services SMOKE_MULTISERVER=1 SMOKE_MULTISERVER_AGE_GATE=1 npm run deploy:test:smoke`
- Result: `PASS` (`[deploy-test-smoke] done`).

Проверки (ключевые):
- `smoke:sso` -> `PASS`.
- `smoke:api` -> `PASS`.
- `smoke:auth:session` -> `PASS`.
- `smoke:auth:cookie-negative` -> `PASS`.
- `smoke:auth:cookie-ws-ticket` -> `PASS`.
- `smoke:web:version-cache` -> `PASS` (sha=`ea1cb3c...`).
- `smoke:web:crash-boundary:browser` -> `PASS`.
- `smoke:web:rnnoise:browser` -> `PASS`.
- `smoke:desktop:update-feed` -> `PASS`.
- `smoke:multiserver` -> `PASS`.
- `smoke:multiserver:age-gate` -> `PASS` (`mode=no-room-skip`).
- `smoke:multiserver:role-matrix` -> `PASS` (`roles=admin,member,owner`).
- `smoke:realtime` -> `PASS` (`ok: true`).

Notes:
- На health-check был кратковременный `502`, затем API восстановился автоматически, прогон завершился штатно.
- Кнопка `Удалить сервер` добавлена в UI, backend endpoint удаления сервера пока не реализован (кнопка возвращает информирующий toast).

## Admin tabs regroup + observability unified page (test, feature branch)

- Date: `2026-03-28`
- Branch: `origin/feature/multiserver-stage1-services`
- Applied SHA: `391d4d581e66cb30e07523a9c42998ee777fc0ae`
- Scope:
  - объединение `Лог` + `Сигналинг` + `Телеметрия` в одну страницу с внутренними табами;
  - перенос server control plane списка в `Управление продуктом` с табами `Пользователи` и `Сервера`;
  - переименование `Управление серверами` в `Этот сервер`;
  - унификация action-кнопок в строке участников по паттерну product management.
- Command (server):
  - `TEST_REF=origin/feature/multiserver-stage1-services SMOKE_MULTISERVER=1 SMOKE_MULTISERVER_AGE_GATE=1 npm run deploy:test:smoke`
- Result: `PASS` (`[deploy-test-smoke] done`).

Проверки (ключевые):
- `smoke:sso` -> `PASS`.
- `smoke:api` -> `PASS`.
- `smoke:auth:session` -> `PASS`.
- `smoke:auth:cookie-negative` -> `PASS`.
- `smoke:auth:cookie-ws-ticket` -> `PASS`.
- `smoke:web:version-cache` -> `PASS` (sha=`391d4d5...`).
- `smoke:web:crash-boundary:browser` -> `PASS`.
- `smoke:web:rnnoise:browser` -> `PASS`.
- `smoke:desktop:update-feed` -> `PASS`.
- `smoke:multiserver` -> `PASS`.
- `smoke:multiserver:age-gate` -> `PASS` (`mode=no-room-skip`).
- `smoke:multiserver:role-matrix` -> `PASS` (`roles=admin,member,owner`).
- `smoke:realtime` -> `PASS` (`ok: true`).

Notes:
- На этапе ожидания health был кратковременный `502`, затем API автоматически восстановился и прогон завершился штатно.

## Server rename + age-confirm UI (test, feature branch)

- Date: `2026-03-28`
- Branch: `origin/feature/multiserver-stage1-services`
- Applied SHA: `96d70edd84afa35d09510953e8f1f10f2d876bc2`
- Scope:
  - UI переименования текущего сервера;
  - UI проверки/подтверждения age-gate статуса для текущего сервера;
  - wiring frontend API для `GET/POST /v1/servers/:serverId/age-confirm` и `PATCH /v1/servers/:serverId`.
- Command (server):
  - `TEST_REF=origin/feature/multiserver-stage1-services SMOKE_MULTISERVER=1 SMOKE_MULTISERVER_AGE_GATE=1 npm run deploy:test:smoke`
- Result: `PASS` (`[deploy-test-smoke] done`).

Проверки (ключевые):
- `smoke:sso` -> `PASS`.
- `smoke:api` -> `PASS`.
- `smoke:auth:session` -> `PASS`.
- `smoke:auth:cookie-negative` -> `PASS`.
- `smoke:auth:cookie-ws-ticket` -> `PASS`.
- `smoke:web:version-cache` -> `PASS` (sha=`96d70ed...`).
- `smoke:web:crash-boundary:browser` -> `PASS`.
- `smoke:web:rnnoise:browser` -> `PASS`.
- `smoke:desktop:update-feed` -> `PASS`.
- `smoke:multiserver` -> `PASS`.
- `smoke:multiserver:age-gate` -> `PASS` (`mode=no-room-skip`).
- `smoke:multiserver:role-matrix` -> `PASS` (`roles=admin,member,owner`).
- `smoke:realtime` -> `PASS` (`ok: true`).

Notes:
- Во время ожидания API health был кратковременный `502`, затем health восстановился автоматически и прогон завершился штатно.

## Multi-server Stage 5/6 continuation (test, feature branch)

- Date: `2026-03-28`
- Branch: `origin/feature/multiserver-stage1-services`
- Applied SHA: `9b8b6104c3526134e2a0183ae1af9f463c489238`
- Scope:
  - backend endpoint ownership transfer: `POST /v1/servers/:serverId/owner`;
  - подтверждение стабильности multi-server smoke после фикса role-matrix.
- Command (server):
  - `TEST_REF=origin/feature/multiserver-stage1-services SMOKE_MULTISERVER=1 SMOKE_MULTISERVER_AGE_GATE=1 npm run deploy:test:smoke`
- Result: `PASS` (`[deploy-test-smoke] done`).

Проверки (ключевые):
- `smoke:sso` -> `PASS`.
- `smoke:api` -> `PASS`.
- `smoke:auth:session` -> `PASS`.
- `smoke:auth:cookie-negative` -> `PASS`.
- `smoke:auth:cookie-ws-ticket` -> `PASS`.
- `smoke:web:version-cache` -> `PASS` (sha=`9b8b610...`).
- `smoke:web:crash-boundary:browser` -> `PASS`.
- `smoke:web:rnnoise:browser` -> `PASS`.
- `smoke:desktop:update-feed` -> `PASS`.
- `smoke:multiserver` -> `PASS` (`mode=active-invite-limit-skip`).
- `smoke:multiserver:age-gate` -> `PASS` (`mode=active-invite-limit-skip`).
- `smoke:multiserver:role-matrix` -> `PASS` (`roles=admin,member,owner`).
- `smoke:realtime` -> `PASS` (`ok: true`).

Notes:
- Во время ожидания health наблюдался кратковременный `curl (56) 502`, затем health восстановился, deploy продолжился штатно.
- Для насыщенного test-state smoke корректно использует skip-режимы по лимиту активных invite, не ломая deploy gate.

## Invite link accept UI flow (test, feature branch)

- Date: `2026-03-28`
- Branch: `origin/feature/multiserver-stage1-services`
- Applied SHA: `05722859cc06eb9330247153a24646f639a92f01`
- Scope:
  - web flow для deep-link `/invite/:token`;
  - клиентский вызов `POST /v1/invites/:token/accept`;
  - автопереключение на принятый сервер + очистка URL после обработки invite.
- Command (server):
  - `TEST_REF=origin/feature/multiserver-stage1-services SMOKE_MULTISERVER=1 SMOKE_MULTISERVER_AGE_GATE=1 npm run deploy:test:smoke`
- Result: `PASS` (`[deploy-test-smoke] done`).

Проверки (ключевые):
- `smoke:web:version-cache` -> `PASS` (sha=`0572285...`).
- `smoke:multiserver` -> `PASS`.
- `smoke:multiserver:age-gate` -> `PASS` (`mode=no-room-skip`).
- `smoke:multiserver:role-matrix` -> `PASS` (`roles=admin,member,owner`).
- `smoke:realtime` -> `PASS` (`ok: true`).

## Owner transfer + server unban UI (test, feature branch)

- Date: `2026-03-28`
- Branch: `origin/feature/multiserver-stage1-services`
- Applied SHA: `e6ae8a634c7e5f16cc4c899364f25e2a08e92135`
- Scope:
  - UI кнопка `Transfer ownership` для owner в списке участников;
  - UI кнопка `Unban` для server-banned участников;
  - server member list расширен полем `isServerBanned` для точного состояния кнопок.
- Command (server):
  - `TEST_REF=origin/feature/multiserver-stage1-services SMOKE_MULTISERVER=1 SMOKE_MULTISERVER_AGE_GATE=1 npm run deploy:test:smoke`
- Result: `PASS` (`[deploy-test-smoke] done`).

Проверки (ключевые):
- `smoke:web:version-cache` -> `PASS` (sha=`e6ae8a6...`).
- `smoke:multiserver` -> `PASS`.
- `smoke:multiserver:age-gate` -> `PASS` (`mode=no-room-skip`).
- `smoke:multiserver:role-matrix` -> `PASS` (`roles=admin,member,owner`).
- `smoke:realtime` -> `PASS` (`ok: true`).
