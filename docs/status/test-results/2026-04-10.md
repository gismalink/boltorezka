# Test results — 2026-04-10

## Telegram-like chat execution: read/counter/race stabilization (test)

- Date: `2026-04-10`
- Branch: `origin/feature/ws-version-sync-datute`
- Scope:
  - strict monotonic read pointer + stale read guard;
  - server-first unread counters in chat panel;
  - race send/read/reconnect smoke coverage and browser gap-recovery stability.

### 1) Main deploy smoke (server/test contour)

```bash
ssh mac-mini 'cd ~/srv/boltorezka && TEST_REF=origin/feature/ws-version-sync-datute npm run deploy:test:smoke'
```

Final green cycle:
- Applied SHA: `8fb3176c6a42cb7bb1ab4fca038118712907644f`
- Result: `PASS`
- Evidence highlights:
  - `smoke:web:gap-recovery:browser` = `ok` (`ws.realtime.gap.detected`, `ws.realtime.gap.recovered`)
  - `smoke:chat:anchor-jump` = `ok`
  - `smoke:realtime` = `ok`

### 2) Targeted realtime race validation (fresh two-user bootstrap)

```bash
ssh mac-mini 'cd ~/srv/boltorezka && \
  SMOKE_AUTH_COMPOSE_FILE=infra/docker-compose.host.yml \
  SMOKE_AUTH_ENV_FILE=infra/.env.host \
  SMOKE_AUTH_API_SERVICE=boltorezka-api-test \
  SMOKE_AUTH_POSTGRES_SERVICE=boltorezka-db-test \
  SMOKE_AUTH_TOTAL_USERS=2 \
  SMOKE_API_URL=https://test.datowave.com \
  SMOKE_AUTH_OUTPUT_FILE=.deploy/smoke-auth-race.env \
  bash ./scripts/smoke/smoke-auth-bootstrap.sh && \
  set -a && source .deploy/smoke-auth-race.env && set +a && \
  SMOKE_API_URL=https://test.datowave.com SMOKE_RECONNECT=1 npm run smoke:realtime'
```

Result:
- Applied SHA during validation: `8fb3176c6a42cb7bb1ab4fca038118712907644f`
- `reconnectReadRaceChecked=true`
- `reconnectReadRaceOk=true`
- `reconnectDriftChecked=true`
- `reconnectDriftOk=true`

### 3) Notes on failed intermediate runs

- Intermediate fails at SHAs `4aeb9af`, `20bb088`, `81b8ad9` were reproduced and fixed in the same branch.
- Root causes were smoke-level determinism issues (`smoke:web:gap-recovery:browser` timing and reconnect race fixture ordering), not regression in deploy/test contour.

### 4) Acceptance status snapshot

- Plan reference:
  - `docs/plans/2026-04-09_TELEGRAM_LIKE_CHAT_EXECUTION_PLAN.md`
- Status on `2026-04-10`:
  - `2.2.1` strict monotonic read pointer: closed
  - `2.2.2` server-first counters: closed
  - `2.2.3` race send/read/reconnect tests: closed
