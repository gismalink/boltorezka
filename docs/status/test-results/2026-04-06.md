# Test results — 2026-04-06

## AI-agent-ready accessibility pilot: browser semantics gate (test)

- Date: `2026-04-06`
- Branch: `origin/feature/ai-agent-ready-accessibility`
- Scope:
  - semantic contract coverage for pilot chat flows (timeline/composer/topic nav/search/overlays);
  - deterministic action feedback (`requested/accepted/failed:<reason>`);
  - modal semantics coverage in pilot scope (chat profile modal + user settings/delete-confirm + room member profile modal).

### 1) Automated test + evidence command (server/test contour)

```bash
SMOKE_API_URL=https://test.datowave.com \
SMOKE_WEB_BASE_URL=https://test.datowave.com \
SMOKE_TEST_BEARER_TOKEN=<token> \
npm run smoke:web:agent-semantics:evidence
```

По умолчанию команда не пишет изменения в этот markdown-файл (чтобы не загрязнять git-ветку на сервере).

Если нужно именно авто-обновить блок `Run result`, запускайте явно с флагом:

```bash
SMOKE_EVIDENCE_WRITE_DOC=1 \
SMOKE_API_URL=https://test.datowave.com \
SMOKE_WEB_BASE_URL=https://test.datowave.com \
SMOKE_TEST_BEARER_TOKEN=<token> \
npm run smoke:web:agent-semantics:evidence
```

### 2) Planned integrated gate command (optional full flow)

```bash
SMOKE_API_URL=https://test.datowave.com \
SMOKE_WEB_BASE_URL=https://test.datowave.com \
SMOKE_TEST_BEARER_TOKEN=<token> \
SMOKE_E2E_AGENT_SEMANTICS_BROWSER=1 \
npm run smoke:web:e2e
```

### 3) Expected selectors coverage (summary)

- Core chat screen:
  - `chat.panel`
  - `chat.screen-context.status`
  - `chat.timeline`
  - `chat.composer`
  - `chat.composer.input`
  - `chat.composer.submit`
- Topic/search/navigation:
  - `chat.topic-navigation`
  - `chat.topic-navigation.search-toggle`
  - `chat.search.panel`
  - `chat.search.query`
  - `chat.search.scope`
  - `chat.search.filters`
- Optional overlays/context:
  - `chat.overlay.topic-palette*`
  - `chat.topic-context-menu*`
- Optional settings modal path:
  - `userdock.voice-settings.toggle`
  - `settings.user-modal.open`
  - `settings.user-modal`
  - `settings.user-modal.close`

### 4) Run result (manual or explicit auto-update)

- Applied SHA: `7e629fc`
- Result: `PASS`
- Output excerpt:
  - `[smoke:web:agent-semantics:browser] pass|ok`
  - `- verified selectors: 12`
- Notes:
  - `auto-updated by smoke:web:agent-semantics:evidence`

### 5) Acceptance mapping

- Plan reference:
  - `docs/plans/2026-04-06_AI-Agent_Ready_Accessibility_Implementation_PLAN.md`
- Acceptance criteria impacted:
  - accessibility tree-first navigation in pilot flows;
  - machine-readable action feedback;
  - smoke confirmation of stable agent-ready contract in `test`.
