# Test results — 2026-03-26

## Domain cutover continuation (datowave)

- Branch: `feature/datowave-auth-stack-move`
- SHA: `47e8efbe9a8b98211f5925d5f95cb13509876ea8`
- Command (server): `ssh mac-mini 'cd ~/srv/boltorezka && export TEST_REF=origin/feature/datowave-auth-stack-move && npm run deploy:test:smoke'`
- Result: `PASS`

## Key checks

- Deploy test contour: PASS (`api-only + caddy static sync`).
- Auth/SSO routing smoke: PASS (`test.datowave.com -> test.auth.datowave.com`).
- API smoke: PASS.
- Auth session lifecycle smoke: PASS (`sid` present).
- Cookie negative + ws-ticket smokes: PASS.
- Web version/cache smoke: PASS (`sha=47e8efbe9a8b98211f5925d5f95cb13509876ea8`).
- Realtime smoke: PASS (`mediaTopology=livekit`, reconnect/call relay path OK).
- TURN allocation failures (30m): `0`.

## Redirect map smoke

- Command (server): `ssh mac-mini 'cd ~/srv/boltorezka && npm run smoke:redirect-map'`
- Result: `PASS`
- Cases:
	- `test.boltorezka.gismalink.art` -> `test.datowave.com` (`308`)
	- `test.datute.ru` -> `test.datowave.com` (`308`)
- Path/query preservation: confirmed by smoke script assertions.

## Auth dual-host check (test)

- Command (server): `ssh mac-mini 'for u in https://test.auth.datowave.com https://test.auth.gismalink.art; do curl -sSI "$u"; done; ...'`
- Result: `PASS`
- `https://test.auth.datowave.com` отвечает напрямую (`HTTP 404` на root, без redirect).
- `https://test.auth.gismalink.art` отвечает напрямую (`HTTP 404` на root, без redirect).
- Provider route check:
	- `https://test.auth.datowave.com/auth/google?...` -> `302` на Google OAuth.
	- `https://test.auth.gismalink.art/auth/google?...` -> `302` на Google OAuth.
- Вывод: old/new auth-host работают параллельно (dual-host), принудительного redirect со старого auth-host нет.

## Notes

- Local запуск `deploy:test:smoke` в рабочем каталоге без `infra/.env.host` ожидаемо не проходит; валидный gate выполнен на сервере в test-контуре.
